@{
    ViewBag.Title = "Home";
}

<div class="container text-center">
    <div class="row">
        <div class="col-md-12">
            <h1 class="display-4 mb-4">Calendar App</h1>
            <p class="lead mb-4">Sistema de gestión de eventos y calendario</p>
            <a href="@Url.Action("Index", "Calendar")" class="btn btn-primary btn-lg">
                <i class="fas fa-calendar-alt me-2"></i>Ir al Calendario
            </a>
        </div>
    </div>
</div>